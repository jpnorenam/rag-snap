# Development to-do list:

* [ ] Ingest from Urls and GSuite docs.
* [ ] Improve prompts in `rag-snap/cmd/cli/basic/chat/rag.go`
* [ ] Add a batch command to extract questions from a document and answer them individually.
* [ ] Allow to export and import knowledge bases.
* [ ] Add go `test-unit` for the go packages
* [ ] Add go `test-style` for the go packages, using the `golangci-lint` package/snap.
* [ ] Create the usage docs and contribution guide

# Testing to-do list before v0.0.1 release:

* [ ] Create an extensive knowledge base
* [ ] Ingest documents on other formats (xslx, docx, pptx)
